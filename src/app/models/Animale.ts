// LA DEFINIZIONE DI UN TIPO DI DATO CUSTOM (MODEL), SI PUO' FARE CON: TYPE, INTERFACE, CLASS
export type Animale = {
    nome: string,
    razza: string
}

export interface IAnimale {
    nome: string,
    razza: string
}